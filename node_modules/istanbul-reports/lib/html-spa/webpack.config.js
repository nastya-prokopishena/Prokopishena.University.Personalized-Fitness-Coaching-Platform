'use strict';

const path = require('path');

module.exports = {
    mode: 'development',
    entry: path.resolve(__dirname, 'Prokopishena.University.Personalized-Fitness-Coaching-Platform.Web/server.js'),
    output: {
        path: path.resolve(__dirname, 'assets'),
        filename: 'bundle.js'
    },
    module: {
        rules: [
            {
                test: /\.js$/,
                exclude: /node_modules/,
                use: {
                    loader: 'babel-loader'
                }
            }
        ]
    }
};
